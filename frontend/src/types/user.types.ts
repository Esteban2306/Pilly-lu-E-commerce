export type UserType = {
    _id: string;
    id?: string;
    email: string;
    role: string
    firstName: string;
    lastName: string
}

export type UserRow = {
    _id: string;
    name: string;
    email: string;
    role: string;
    createdAt: string;
};